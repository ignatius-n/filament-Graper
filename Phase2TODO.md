1. doesn't the tailwind plugin in tailwind bring in its own blcoks? 
2. if yes lets keep it to those blocks instead of our custom ones
3. users should be able to add custom blocks, the custom ones should be flexible as well as have these two functionalities Block categories and ordering
   Block preview thumbnails
4. we need documentation now that later 
5. this is my first time creatin a filamentplugin so explain to me how the code all works and how this plugin works with a destail understanding of the code. create an md file of thi and place it on the desktop
6. remove the flow bite and shadcn stuff for now we will do that in future
7. When I make a change how do i get it to update in the app that that testing it in?
8. Perhaps we should be using Alpine like https://github.com/dotswan/filament-grapesjs-v3
